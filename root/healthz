#!/bin/sh

# Health check for Bulwark Webmail. Checks if the server is responding on localhost:3000.
if ! fetch -qo /dev/null --timeout 5 "http://127.0.0.1:3000/" 2>/dev/null; then
    echo "Bulwark Webmail not responding"
    exit 1
fi

echo "healthy"
exit 0